<%= form_for(@task) do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name, placeholder: 'type your task...' %>

  <% if @task.persisted? %>
    <%= f.label :status %>
    <%= f.check_box :status %>
  <% end %>

  <%= f.submit %>
<% end %>